# FlumenData Tier 2: Analytics & Development Platform
# Services: JupyterLab
# Dependencies: Tier 0 (PostgreSQL, MinIO) + Tier 1 (Hive, Spark)

services:
  # =============================================================================
  # JupyterLab - Interactive Development Environment
  # =============================================================================
  jupyterlab:
    container_name: flumen_jupyterlab
    build:
      context: .
      dockerfile: docker/jupyterlab.Dockerfile
      args:
        - HADOOP_AWS_VERSION=${HADOOP_AWS_VERSION}
        - AWS_SDK_BUNDLE_VERSION=${AWS_SDK_BUNDLE_VERSION}
        - POSTGRESQL_JDBC_VERSION=${POSTGRESQL_JDBC_VERSION}
        - DELTA_VERSION=${DELTA_VERSION}
        - SCALA_BINARY_VERSION=${SCALA_BINARY_VERSION}
    image: flumendata/jupyterlab:spark-4.0.1
    hostname: jupyterlab
    ports:
      - "8888:8888"
    environment:
      - JUPYTER_ENABLE_LAB=yes
      - JUPYTER_TOKEN=''
      - JUPYTER_ALLOW_INSECURE_WRITES=yes
      - GRANT_SUDO=yes
      - CHOWN_HOME=yes
      - CHOWN_HOME_OPTS=-R
      # Spark configuration
      - SPARK_HOME=/usr/local/spark
      - PYSPARK_PYTHON=/usr/bin/python3
      - PYSPARK_DRIVER_PYTHON=/opt/conda/bin/python
      # MinIO S3 credentials
      - AWS_ACCESS_KEY_ID=${MINIO_ROOT_USER}
      - AWS_SECRET_ACCESS_KEY=${MINIO_ROOT_PASSWORD}
      - AWS_ENDPOINT_URL=${MINIO_SERVER_URL}
      - AWS_REGION=us-east-1
    volumes:
      # Notebooks and work directory (persistent - can be a git repo!)
      - ${DATA_DIR}/notebooks:/home/jovyan/work
      # Spark configuration (runtime override)
      - ./config/jupyterlab/spark-defaults.conf:/usr/local/spark/conf/spark-defaults.conf:ro
      # Sample notebooks (read-only reference)
      - ./templates/jupyterlab/notebooks/01_quickstart.ipynb:/home/jovyan/work/_examples/01_quickstart.ipynb:ro
    depends_on:
      spark-master:
        condition: service_healthy
      hive-metastore:
        condition: service_healthy
    healthcheck:
      test: ["CMD", "curl", "-f", "http://localhost:8888/lab"]
      interval: 30s
      timeout: 10s
      retries: 3
      start_period: 30s
    restart: unless-stopped

# =============================================================================
# Named Volumes - Using bind mount for notebooks only
# =============================================================================
# Notebooks: ${DATA_DIR}/notebooks (bind mount - version control this!)

# =============================================================================
# Networks
# =============================================================================
networks:
  default:
    external: true
    name: flumendata_default
