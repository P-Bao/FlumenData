services:
  upload-api:
    build:
      context: ./api
      dockerfile: ../docker/api.Dockerfile
    container_name: flumen_api
    restart: unless-stopped

    ports:
      - "12397:12397"

    networks:
      - flumendata_default

networks:
  flumendata_default:
    external: true
