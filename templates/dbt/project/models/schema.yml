version: 2

models:
  - name: example
    description: "Demonstration model that materializes a simple view in PostgreSQL."
    columns:
      - name: id
        description: "Synthetic identifier for example row."
        tests:
          - not_null
          - unique
