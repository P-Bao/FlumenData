# Trino catalog: metrics (PostgreSQL connector → metrics_db)
# Lưu file này vào: docker/trino/catalog/metrics.properties
# (hoặc mount vào /etc/trino/catalog/metrics.properties trong container)

connector.name=postgresql
connection-url=jdbc:postgresql://postgres:5432/metrics_db
connection-user=admin
connection-password=admin123
